<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//org.mybatis//DTD Mapper 3.0//EN"
        "http://org.mybatis/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shineyue.demo.mapper.TeacherMapper">
    <select id="load" parameterType="String" resultType="Teacher">
        select * from t_teacher
        <where>
            id=#{_parameter}
        </where>
    </select>
</mapper>